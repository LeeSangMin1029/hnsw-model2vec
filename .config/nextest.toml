[profile.default]
status-level = "fail"
# Skip lindera-dependent korean tests (8s dict load per process in nextest).
# Mock tests run without gate. Real dict tests: cargo nextest r --profile korean
default-filter = "not (package(v-hnsw-search) & test(/korean_tokenizer/))"

[profile.korean]
status-level = "pass"
